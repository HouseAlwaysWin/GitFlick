using Avalonia.Controls;

namespace GitFlick.Views;

/// <summary>Global settings (language + theme), opened non-modally from the palette ⚙ button.</summary>
public partial class SettingsWindow : Window
{
    public SettingsWindow()
    {
        InitializeComponent();
    }
}
